namespace Wondeluxe.Tweening
{
	/// <summary>
	/// Encapsulates a method that handles <see cref="member">Tween</see> events.
	/// </summary>
	/// <param name="tween">The Tween object that invoked the event.</param>

	public delegate void TweenEventHandler(Tween tween);
}